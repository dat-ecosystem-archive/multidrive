{
  "name": "multidrive",
  "version": "1.0.1",
  "description": "Manage multiple hyperdrives in a central location",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps",
    "test:cov": "standard && npm run deps"
  },
  "repository": "yoshuawuyts/multidrive",
  "keywords": [
    "hyperdrive",
    "hypercore",
    "dat"
  ],
  "license": "MIT",
  "dependencies": {
    "concat-stream": "^1.5.2",
    "hyperdrive": "^7.8.0",
    "named-level-store": "^1.0.0",
    "pump": "^1.0.1"
  },
  "devDependencies": {
    "dependency-check": "^2.6.0",
    "istanbul": "^0.4.5",
    "standard": "^8.5.0",
    "tape": "^4.6.2"
  }
}
